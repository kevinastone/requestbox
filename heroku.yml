setup:
  addons:
    - plan: heroku-postgresql
  config:
    MIX_ENV: prod
build:
  docker:
    web: Dockerfile
# release:
#   image: web
#   command:
#     - mix ecto.setup
run:
  web: mix phx.server
